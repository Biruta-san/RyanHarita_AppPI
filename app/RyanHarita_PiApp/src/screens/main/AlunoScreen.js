import { SafeAreaView } from "react-native-safe-area-context";

const AlunoScreen = () => {
  return (
    <SafeAreaView>
      <Text>123</Text>
    </SafeAreaView>
  );
};

export default AlunoScreen;
